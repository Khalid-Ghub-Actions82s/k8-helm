apiVersion: v1
kind: service
metadata:
  name: nginx-lb
spec:
  type: {{ .Values.service.type }}
  selector:
    project: roboshop
    purpose: helm-demo
  ports:
  -  protocal: TCP
     port: {{ .Values.service.servicePort }}
     targetport: 80
     nodePort: 30007